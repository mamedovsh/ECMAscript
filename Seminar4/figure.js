// const dogImageContainer = document.querySelector("div.dogImageContainer");

// fetchData.forEach((element) => {
// dogImageContainer.insertAdjacentHTML(
// "beforeend",
// `
// <figure>
// <img src="${element.url}" />
// <figcaption>${element.explanation}</figcaption>
// </figure>
// `
// );
// });